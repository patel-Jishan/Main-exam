/*C program to find sum of elements of a matrix*/

#include<stdio.h>
int main(){
int i,j,row,column,sum;

printf("Enter the row of a matrix:");
scanf("%d",&row);

printf("Enter the column of a matrix:");
scanf("%d",&column);
int arr[row column],arr[row column];
printf("Enter the rows :")
for(i=row;i<=row;i++){
	for(j=column;j<=column;j++){
		printf("%d",arr x[i][j]);
		
	
	}
}
printf("Enter the column :")
for(i=row;i<=row;i++){
	for(j=column;j<=column;j++){
		printf("%d",arr y[i][j]);
		
	
	}
}
	sum=sum(row+column);
		printf("%d",sum);
		
	}
